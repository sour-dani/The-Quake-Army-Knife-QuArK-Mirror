title: CloseLogFile
author: DanielPharos

<code>
procedure CloseLogFile;
</code>

This procedure closes the logfile. It writes the current date and time to the logfile, and closes it.

It is safe to call this procedure is the logfile is already closed; it will then do nothing.
